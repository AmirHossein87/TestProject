IF OBJECT_ID('[DatabaseVersioning].[TableProcess_Refresh]') IS NOT NULL
	DROP PROCEDURE [DatabaseVersioning].[TableProcess_Refresh];

GO
SET QUOTED_IDENTIFIER ON
GO
SET ANSI_NULLS ON
GO
CREATE PROCEDURE [DatabaseVersioning].[TableProcess_Refresh]
AS
BEGIN
   
   -- Obviously database versioning feaure must checked all necessary requiments



   -- Get source structure 

   -- Get current db structure

   RETURN

    
END
GO
